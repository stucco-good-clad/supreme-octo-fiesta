[package]
name = "libcdn_fetch"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
reqwest = { version = "0.12", features = ["http2", "native-tls"] }
tokio = { version = "1", features = ["rt", "rt-multi-thread"] }
